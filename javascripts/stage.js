var stage = window.stage || {};

stage.container = document.getElementById('app_stage');

stage.setup = function() {
  
};

stage.reset = {
  
};

stage.render = function(){
  stage.setup();
};

stage.init = function() {
  stage.render();
};